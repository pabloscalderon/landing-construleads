---
// Kicker de sección con el guion decorativo del acento (—•—) delante.
interface Props {
  texto: string;
  centrado?: boolean;
}
const { texto, centrado = false } = Astro.props as Props;
---
<p class:list={["flex items-center gap-2.5 text-xs font-medium uppercase tracking-[0.18em] text-suave", centrado && "justify-center"]}>
  <span aria-hidden="true" class="inline-flex items-center gap-1 text-primario">
    <span class="block h-px w-4 bg-primario"></span>
    <span class="block h-1 w-1 rounded-full bg-primario"></span>
    <span class="block h-px w-4 bg-primario"></span>
  </span>
  <span>{texto}</span>
</p>
